# OASIS-VITE 프로젝트 (OATIS)

열린학원 학원 관리 시스템. React + Vite + Firebase Firestore.

## 메모리

프로젝트 메모리는 `.claude/memory/` 폴더에 있음. 새 세션 시작 시 반드시 확인.

## 배포

배포는 `firebase deploy` 직접 실행. GitHub Actions 사용 안 함.
git push는 코드 관리 목적으로만 사용.
