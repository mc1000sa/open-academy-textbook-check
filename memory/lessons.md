# 열린학원 수학교재점검 시스템 누적 학습 노트

이 파일은 개발 과정의 시행착오, 사용자 선호, 반복 실수를 기록하는 장기 기억 저장소다. 새 세션을 시작하면 `RULES.md`와 함께 반드시 읽는다.

## 1. 사용자 선호와 제품 방향

- 사용자는 프로젝트 시작 전에 개발 방향을 먼저 정하고, 그 방향을 프로젝트 폴더 안의 문서와 폴더 구조로 고정하고 싶어 한다.
- 강사용 교재 점검 입력은 빠르고 직관적이어야 한다. 타이핑보다 버튼 클릭, 선택식 점수, 자동 계산을 우선한다.
- 학부모 보고서는 말로 길게 설명하기보다 막대그래프와 6각형 비교처럼 한눈에 이해되는 시각화를 선호한다.
- 반별 전체 보고서는 감이 아니라 수치로 반의 교재 완성도와 학생별 상태를 비교해야 한다.
- 사용자는 앞으로 새 웹앱 프로젝트를 시작할 때 `AGENTS.md`, `RULES.md`, `memory/lessons.md`, `docs/` 구조를 먼저 만들고 진행하고 싶어 한다.
- 사용자는 처음에는 빠르게 목업 웹을 만들 수 있지만, 핵심 화면과 반복 UI가 보이면 AI가 먼저 Vite/React 분리 시점을 제안해주길 원한다.
- 사용자는 분리 시점을 직접 판단하기 어려워하므로, AI가 "지금 분리해야 하는 이유"와 "분리 후 구조"를 설명하고 승인 후 오류 없이 분리한 다음 작업을 이어가야 한다.
- 관리자 페이지는 항상 섹션별로 카드 형식으로 관리자 메뉴를 정리하고, 카드는 최초 진입 시 접기(collapsed) 형식으로 구성하여 불필요한 스크롤을 줄인다.
- 시스템 내 모든 주요 설정 및 텍스트(예: 로그인/스플래시 화면 등)는 하드코딩하지 않고 관리자 메뉴에서 실시간 조정 및 DB 연동이 가능하도록 반영한다.

## 2. 현재 프로젝트에서 확인한 사실

- 현재 저장소는 Vite/React 구조로 전환되었다.
- 기존 앱 코드는 `src/legacy/legacyApp.js`와 `src/styles/legacy.css`로 분리되었고, React 컴포넌트가 직접 마운트한다.
- 새 작업 위치는 `src/components`, `src/lib`, `src/styles`다.
- `firebase.json`의 hosting public 경로는 Vite 빌드 결과물인 `dist`다.
- 현재 루트 `index.html`은 Tailwind CDN과 Font Awesome CDN을 로드하고, Firebase는 npm 패키지를 사용한다.
- 기존 샘플 `RULES.md`에는 React/Vite와 Tailwind 금지 규칙이 있었지만, 이 프로젝트에는 그대로 적용하면 안 된다.

## 3. 자주 조심할 점

- 기존 Firestore 데이터와 컬렉션을 깨지 않도록 새 필드는 하위 호환으로 추가한다.
- 보고서 출력 기능은 화면 표시와 인쇄/PDF 저장을 모두 고려한다.
- 사용자가 "편하게 입력"을 강조하면, 입력 필드 추가보다 버튼식 UX를 먼저 설계한다.
- 단원별 완료율, 학생 누적 점수, 반 평균은 계산 기준을 문서화하고 재현 가능하게 만든다.
- 단일 HTML/목업 파일이 커지는 동안 기능을 계속 추가하면 나중에 분리 오류가 커지므로, 로그인/대시보드/입력/보고서 같은 주요 화면이 보이는 즉시 분리 제안을 검토한다.
- **브라우저 기본 팝업(alert, confirm, prompt)은 절대 사용하지 않는다.** 반드시 프로젝트 톤앤매너에 맞는 커스텀 모달(`showModalAlert`, `showModalConfirm`, `showModalPrompt`)을 사용한다. 이 규칙을 어기면 즉시 교체한다.
- 관리자 메뉴 내 카드(아코디언) UI는 프로젝트 다크 테마(`bg-slate-900`, `border-slate-800`, `text-slate-200` 등)와 완전히 일치해야 한다. 흰색 배경(`bg-white`)과 밝은 테마를 카드 내부에 하드코딩하지 않는다.
- setupView.js, bookSetupView.js 등 뷰 파일의 버튼에는 반드시 명확한 hover 색상 변화(`hover:bg-xxx hover:text-white transition-colors`)를 추가한다.
- 반(class) "수정" 버튼은 폼에 기존 값을 채우고 1번 아코디언을 열어주는 방식으로 구현한다. 별도 수정 모달을 만들지 않는다.
- 과제 점검 개선에서는 과거 점검 원본을 수정하지 않고, 새 점검 기록의 `carryoverResolutions`에 지난 미완료 회수 이력을 저장한다.
- 6요소 명칭은 `과제 수행률`, `풀이 표현력`, `채점 성실도`, `수업 태도`, `개념 이해도`, `응용 해결력`을 사용한다.
- 이번 회차 과제 수행률과 지난 미완료 회수율은 합산하지 않고 별도 지표로 표시한다.

## 4. 업데이트 규칙

# 열린학원 수학교재점검 시스템 누적 학습 노트

이 파일은 개발 과정의 시행착오, 사용자 선호, 반복 실수를 기록하는 장기 기억 저장소다. 새 세션을 시작하면 `RULES.md`와 함께 반드시 읽는다.

## 1. 사용자 선호와 제품 방향

- 사용자는 프로젝트 시작 전에 개발 방향을 먼저 정하고, 그 방향을 프로젝트 폴더 안의 문서와 폴더 구조로 고정하고 싶어 한다.
- 강사용 교재 점검 입력은 빠르고 직관적이어야 한다. 타이핑보다 버튼 클릭, 선택식 점수, 자동 계산을 우선한다.
- 학부모 보고서는 말로 길게 설명하기보다 막대그래프와 6각형 비교처럼 한눈에 이해되는 시각화를 선호한다.
- 반별 전체 보고서는 감이 아니라 수치로 반의 교재 완성도와 학생별 상태를 비교해야 한다.
- 사용자는 앞으로 새 웹앱 프로젝트를 시작할 때 `AGENTS.md`, `RULES.md`, `memory/lessons.md`, `docs/` 구조를 먼저 만들고 진행하고 싶어 한다.
- 사용자는 처음에는 빠르게 목업 웹을 만들 수 있지만, 핵심 화면과 반복 UI가 보이면 AI가 먼저 Vite/React 분리 시점을 제안해주길 원한다.
- 사용자는 분리 시점을 직접 판단하기 어려워하므로, AI가 "지금 분리해야 하는 이유"와 "분리 후 구조"를 설명하고 승인 후 오류 없이 분리한 다음 작업을 이어가야 한다.
- 관리자 페이지는 항상 섹션별로 카드 형식으로 관리자 메뉴를 정리하고, 카드는 최초 진입 시 접기(collapsed) 형식으로 구성하여 불필요한 스크롤을 줄인다.
- 시스템 내 모든 주요 설정 및 텍스트(예: 로그인/스플래시 화면 등)는 하드코딩하지 않고 관리자 메뉴에서 실시간 조정 및 DB 연동이 가능하도록 반영한다.

## 2. 현재 프로젝트에서 확인한 사실

- 현재 저장소는 Vite/React 구조로 전환되었다.
- 기존 앱 코드는 `src/legacy/legacyApp.js`와 `src/styles/legacy.css`로 분리되었고, React 컴포넌트가 직접 마운트한다.
- 새 작업 위치는 `src/components`, `src/lib`, `src/styles`다.
- `firebase.json`의 hosting public 경로는 Vite 빌드 결과물인 `dist`다.
- 현재 루트 `index.html`은 Tailwind CDN과 Font Awesome CDN을 로드하고, Firebase는 npm 패키지를 사용한다.
- 기존 샘플 `RULES.md`에는 React/Vite와 Tailwind 금지 규칙이 있었지만, 이 프로젝트에는 그대로 적용하면 안 된다.

## 3. 자주 조심할 점

- 기존 Firestore 데이터와 컬렉션을 깨지 않도록 새 필드는 하위 호환으로 추가한다.
- 보고서 출력 기능은 화면 표시와 인쇄/PDF 저장을 모두 고려한다.
- 사용자가 "편하게 입력"을 강조하면, 입력 필드 추가보다 버튼식 UX를 먼저 설계한다.
- 단원별 완료율, 학생 누적 점수, 반 평균은 계산 기준을 문서화하고 재현 가능하게 만든다.
- 단일 HTML/목업 파일이 커지는 동안 기능을 계속 추가하면 나중에 분리 오류가 커지므로, 로그인/대시보드/입력/보고서 같은 주요 화면이 보이는 즉시 분리 제안을 검토한다.
- **브라우저 기본 팝업(alert, confirm, prompt)은 절대 사용하지 않는다.** 반드시 프로젝트 톤앤매너에 맞는 커스텀 모달(`showModalAlert`, `showModalConfirm`, `showModalPrompt`)을 사용한다. 이 규칙을 어기면 즉시 교체한다.
- 관리자 메뉴 내 카드(아코디언) UI는 프로젝트 다크 테마(`bg-slate-900`, `border-slate-800`, `text-slate-200` 등)와 완전히 일치해야 한다. 흰색 배경(`bg-white`)과 밝은 테마를 카드 내부에 하드코딩하지 않는다.
- setupView.js, bookSetupView.js 등 뷰 파일의 버튼에는 반드시 명확한 hover 색상 변화(`hover:bg-xxx hover:text-white transition-colors`)를 추가한다.
- 반(class) "수정" 버튼은 폼에 기존 값을 채우고 1번 아코디언을 열어주는 방식으로 구현한다. 별도 수정 모달을 만들지 않는다.
- 과제 점검 개선에서는 과거 점검 원본을 수정하지 않고, 새 점검 기록의 `carryoverResolutions`에 지난 미완료 회수 이력을 저장한다.
- 6요소 명칭은 `과제 수행률`, `풀이 표현력`, `채점 성실도`, `수업 태도`, `개념 이해도`, `응용 해결력`을 사용한다.
- 이번 회차 과제 수행률과 지난 미완료 회수율은 합산하지 않고 별도 지표로 표시한다.

## 4. 업데이트 규칙

- 사용자가 새 규칙을 알려주면 이 파일에 즉시 반영한다.
- 해결한 버그, 깨진 UI 원인, Firebase 데이터 이슈는 짧게 기록한다.
- 오래되어 틀린 내용은 남겨두지 말고 수정한다.
- 사용자는 크레딧 소모를 매우 신경 쓴다. 큰 기능 작업 후에는 무거운 전체 빌드/전체 브라우저 검증보다 `node --check` 같은 가벼운 확인을 우선하고, 시간이 오래 걸리거나 크레딧이 큰 검증은 사용자에게 확인 요청을 먼저 한다.
- 보고서 출력 메뉴는 기준일 → 반 → 학생 → 생성 가능 회차 순서로 선택한다. 보고서 생성 버튼을 따로 두지 않고 회차 버튼 선택 즉시 미리보기를 생성하며, 웹 미리보기는 기존 다크 카드형 보고서를 유지하고 PNG 저장만 밝은 학부모 전송용 보고서로 만든다.
- 학부모 보고서의 핵심 기능인 개인 PNG 이미지 저장 기능 오류가 해결되어 정상 동작함.
- 반별 일괄 이미지 다운로드는 크롬 다중 다운로드 방식 대신 안정적인 ZIP 파일 일괄 다운로드 방식으로 변경하여 해결 완료함.
- 전체 디자인 고급화는 화려하고 무거운 애니메이션보다 Apple 스타일처럼 여백, 타이포그래피, 미세한 hover/active 반응, 부드러운 Aurora 배경을 활용한 깔끔한 프로페셔널 톤을 선호한다.
- 교재 단원 통계는 교재 목차명과 표준 소단원 ID를 분리해서 관리한다. 표준단원은 너무 세부 개념으로 쪼개지 말고 시중교재 목차 수준에 가깝게 두며, 교재 단원 하나가 여러 표준단원 ID를 연결할 수 있어야 한다.
- 교재 `보관`은 교재 자산 자체를 숨겨두는 개념이고, 반별 `진행 종료`는 특정 반에서만 해당 교재 점검을 끝내는 개념이다. 같은 교재는 다른 반에서 계속 진행할 수 있어야 하므로 반-교재 연결(`classBooks`)의 상태로 관리한다.
- 학생 포털(`studentPortalView`)에서 최근 점검 기록이 결석(`absent`) 또는 교재 미지참(`no_book`)인 경우, 학습 완료율 게이지와 수치는 이전 정상 점검(이전 활성 로그)의 수치를 유지하여 `null%` 표기를 방어한다. 또한 최근 점검 범위는 `결석` 또는 `교재 미지참`으로 표시하고, 코멘트 앞에 `[날짜 상태]` (예: `[2026.05.24 결석]`)를 자동 병입한다. 하단 점검 이력 표에서도 `범위: 결석 (평가 제외)` 형태로 명확히 노출하여 UI 상의 `null` 유출을 완전히 방지한다.
- 과거 전체 파일에 걸쳐 발생했던 한글 깨짐(인코딩) 오류가 모두 해결 및 정리됨.
- **[교재 점검 중복 저장 오류 해결]**:
  - 원인: 모달 내 저장 동작(`executeSave`)이 비동기로 실행되는 도중 '저장 후 다음 학생'을 중복 클릭하면, 첫 번째 클릭으로 인해 교재가 다음 교재(B교재)로 바뀐 상태에서 두 번째 비동기 호출이 연달아 실행되어 B교재에 데이터가 중복 복사 저장되던 현상. 추가로 반별 배정 교재 매핑 시 `b.studentId`라는 존재하지 않는 속성을 검색하여 다음 학생 전환 시 무조건 첫 번째 교재로 튕기는 논리 오류가 있었음.
  - 해결: `isSaving` 로컬 상태 가드를 추가하여 비동기 저장 중에는 모든 저장 관련 버튼을 비활성화하고 중복 실행을 차단함. 또한 다음 학생 전환 시 현재 점검 중이던 교재(`currentBook`)를 정상 유지(반 배정 목록에 있을 때)하도록 교재 매칭 논리를 수정함.
- **[삭제 후 복구/부활 의심 오류 해결]**:
  - 원인: `InspectionsContainer`에서 삭제를 정상 처리(`deleteDoc`)하더라도, 삭제된 기록이 현재 모달이나 에디터에 '수정 모드'로 활성화되어 있던 경우 상위 및 내부의 `state.editingInspectionId`가 삭제된 ID를 가리킨 채 유지됨. 이 상태에서 저장을 누르면 삭제된 ID로 새 `updateDoc`을 시도하거나 다른 데이터 입력 시 덮어쓰여 데이터가 부활한 것처럼 꼬이는 문제 발생.
  - 해결: 삭제가 완료되는 즉시, 삭제된 ID가 현재 수정 중인 ID와 동일하다면 상위 에디터 관련 state를 모두 깨끗하게 초기화(reset)하도록 예외 조치를 추가함.

## 5. 프로젝트 현재 구현 및 분석 상태 (2026-05-28 기준)

- **아키텍처 및 구조**: Vite/React 구조로 변환 완료. 레거시 메인 코드는 `src/legacy/legacyApp.js`와 `src/styles/legacy.css`에 분리 보관 중이며, React 컴포넌트가 직접 이를 마운트해 구동 중임.
- **최근 수정 완료 사항**:
  - 학부모 보고서의 개별 PNG 저장 기능 복구 및 반별 일괄 이미지 다운로드(ZIP 방식) 적용 완료.
  - 학생 포털 결석/미지참 UI 및 상세 이력 표 예외 처리 완료 및 관련 단위 테스트(`studentPortalView.test.js`) 100% 통과.
  - 교재 점검 시 중복 저장되는 치명적인 데이터 꼬임 버그 해결 (`isSaving` 가드 적용 및 교재 매칭 논리 수정).
  - 점검 데이터 삭제 시 수정 중인 ID 상태를 함께 초기화하여 삭제된 데이터 부활/오버라이트 방어 처리 완료.
  - [docs/architecture.md](file:///c:/Users/Dain/Desktop/OASIS-VITE/open-academy-textbook-check/docs/architecture.md)에 누락되었던 7개 라이브러리 및 뷰 구조 현행화 완료.
- **핵심 뷰 구현 현황**:
  - `loginView.js`, `dashboardView.js`, `inspectionsView.js`, `reportsView.js` 등 기능 자체는 거의 완료되었으나 순수 React 컴포넌트([src/components](file:///c:/Users/Dain/Desktop/OASIS-VITE/open-academy-textbook-check/src/components))로의 이관은 진행 중(미완성 상태).
- **진행 중/남은 과제**:
  - 3) 레거시 뷰의 React 컴포넌트 이관.
- **테스트 현황**: 총 82개의 단위 테스트 작동 및 100% 통과.

## 6. 향후 권장 최적화 단계 및 방향 (2026-05-29 수립)

사용자의 크레딧 소모 최소화 요구를 반영하여 수립한 가성비 최적화 단계 로드맵:
1. **[1단계] 렌더링 호출 디바운스(Debounce) 적용**
   - 내용: 여러 컬렉션의 동시 스냅샷(onSnapshot)에 의한 중복 render() 실행(DOM 전체 리빌드)을 requestAnimationFrame 등을 활용해 단일 프레임 1회 실행으로 병합하여 브라우저 부하 최소화.
   - 크레딧 소모: 매우 낮음 (Very Low)
2. **[2단계] Firestore 리스너 및 이벤트 누수(Memory Leak) 방지 클린업 최적화**
   - 내용: LegacyAppHost의 unmount 시점에 legacyApp 내 등록된 onSnapshot 리스너 및 resize 이벤트 리스너가 살아있어 메모리/API 읽기 횟수가 누수되던 현상 방지. unsubscribe 및 cleanup 연동 처리.
   - 크레딧 소모: 낮음 (Low)
3. **[3단계] 학생 포털 진입 시 Firestore 읽기(Read) 비용 및 트래픽 최적화**
   - 내용: 학생 포털 로그인 시 inspections 전체 구독 대신 query 및 where('studentId', '==', studentId) 필터를 걸어 본인 점검 데이터만 받아오도록 튜닝.
   - 크레딧 소모: 보통 (Medium)
4. **[4단계] Firebase SDK Dynamic Import를 적용한 스플래시 로딩 개선**
   - 내용: 초기 로딩 시 무거운 Firebase SDK 패키지를 메인 번들에서 제외하고, 실제 필요한 로그인/API 시점에 비동기 import()로 호출하여 스플래시/게이트웨이 화면의 TTI 단축.
   - 크레딧 소모: 보통 (Medium)
5. **[5단계] CSS/마크업 정리 및 CDN 배제 (Tailwind & FontAwesome 로컬 이관)**
   - 내용: 외부 CDN 스크립트 기반 Tailwind 및 FontAwesome 라이브러리를 로컬 패키지/CSS 빌드 파이프라인으로 흡수하여 외부 네트워크 의존성 차단 및 로딩 성능 최적화.
   - 크레딧 소모: 높음 (High)

